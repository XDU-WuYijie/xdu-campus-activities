# 项目协作约定

## 文档入口
- [功能模块设计](doc/功能模块设计.md)
- [工程进展](doc/工程进展.md)

## 更新规则
1. 每完成一个功能设计，必须同步更新 [功能模块设计](doc/功能模块设计.md)。
2. 每完成一个功能设计或实现里程碑，必须同步更新 [工程进展](doc/工程进展.md)。
3. 设计文档记录模块目标、接口、数据模型、页面入口和边界约束。
4. 进展文档记录当前状态、已完成项、待办项、阻塞项和下一步。
5. 只要确认了数据库表结构改动或新增结构变更脚本，必须同步更新 [init_schema.sql](backend/src/main/resources/db/init/init_schema.sql)，保持初始化脚本与当前最新表结构一致。
6. 前端实现 UI 组件前必须先检查 Ant Design Mobile 是否已有对应能力；存在等价组件时必须优先复用，并通过薄封装、Design Tokens 或样式覆盖适配项目视觉，禁止无必要地从零重写同类交互。
7. 所有前端依赖必须声明在 `frontend/package.json` 并同步提交 `frontend/package-lock.json`；禁止只在本机安装而不更新清单。新机器统一执行 `./scripts/bootstrap.sh`，通过 `npm ci` 按锁文件安装依赖。
8. 前端可复用组件和路由页面必须使用 PascalCase 独立目录，组件实现、局部样式和 `index.ts` 就近维护；`components/ui/`、`components/layout/` 与 `pages/` 根目录只保留子目录和统一导出文件，禁止继续平铺堆放实现文件。
9. `features/<domain>/` 按 lowerCamelCase 业务域组织；域内实现必须拆分到 `api/`、`model/`、`providers/`、`components/` 等职责子目录，并通过各级 `index.ts` 暴露公共能力，禁止在业务域根级平铺实现文件。
10. 前端目录与模块边界由 `npm run lint:structure` 自动校验，并已接入 `npm run lint`；新增模块必须通过该检查，不得绕过或移除结构校验。
11. 前端页面后续迭代必须延续已验收的布局、信息层级、内容顺序和主要交互位置；确需改变现有排版时必须先取得用户确认，并在工程进展中说明差异。
12. 前端用户可见业务路径禁止直接写入中文业务名称；优先使用后端提供的稳定 slug，后端未提供 slug 时使用实体 ID，禁止在前端维护中文名称到英文 slug 的硬编码翻译表，并为迁移期旧链接提供规范化跳转。

## 当前项目方向
- 项目定位为“校园大型活动报名与签到平台”。
- 第一阶段主线是：活动门户、活动详情、发起活动、报名、报名名单、签到码签到。
